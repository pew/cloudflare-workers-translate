{
  "private": true,
  "name": "cloudflare-workers-translate",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Jonas Wagner <boom@confuflux.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.5.1"
  },
  "dependencies": {
    "@aws-sdk/client-translate": "^3.47.1"
  }
}
